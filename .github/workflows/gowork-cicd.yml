name: GoWork schedule Server cleaning

on:
  schedule:
    - cron: "* * * * *"

jobs:

  schedule-cleaning:
    runs-on: ubuntu-latest

    steps:

      - name: Docker all cache cleaning
        run: |
          echo "Hello World!"